# Exemplo de variáveis de ambiente para desenvolvimento
# Aponte o frontend para o proxy local (usado por services/gemini.ts)
VITE_LOCAL_PROXY_URL=http://127.0.0.1:5051/generate

# Opções para autenticação do Google Generative AI (escolha UMA):
# 1) API Key (simples teste local) - NÃO coloque isto em variáveis VITE_ públicas
# API_KEY=YOUR_API_KEY_HERE

# 2) Service Account JSON (recomendado). Baixe o JSON e aponte o caminho:
# GOOGLE_APPLICATION_CREDENTIALS=C:\caminho\para\service-account.json

# Modelo opcional
# GENAI_MODEL=gemini-3-flash-preview
